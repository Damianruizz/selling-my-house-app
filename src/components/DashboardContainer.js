import React from 'react';
import { Card, Typography } from '@material-ui/core';

export const DashboardContainer = props => {

  return (
    <Typography variant="h3">Hola, soy el dashboard container</Typography>
  );
}